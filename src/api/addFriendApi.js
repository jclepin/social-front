const addFriendApi = () => {
  // fetch(`${process.env.REACT_APP_API_URL}/friends`, {
  //   method: "POST",
  //   headers: {
  //     "Content-Type": "application/json",
  //     Authorization: `Bearer ${token}`,
  //   },
  //   body: JSON.stringify({ id1: me.id, id2: who }),
  // })
  //   .then((rawResult) => rawResult.json())
  //   .then((result) => {
  //     dispatch(addFriend({ id: parseInt(who) }));
  //     return true;
  //   });
};

export default addFriendApi;
